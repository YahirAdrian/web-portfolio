{
  "name": "yahir-web-portfolio",
  "version": "1.0.0",
  "description": "Yahir Adrian's web developer portfolio",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "buildcss": "npx @tailwindcss/cli -i ./src/input.css -o ./dist/styles.css --watch",
    "serve": "browser-sync start --server --files \"dist/*.css, *.html\""
  },
  "author": "Yahir Adrian",
  "license": "ISC",
  "dependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}
